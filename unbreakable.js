function split(str, sep = ",") {
let res = [];
if (sep === "") {
    for (let i = 0; i < str.length ; i++) {
        res.push(str[i])
    }
    return res
}
for (let i = 0;  i < str.length; i++)
    if (str[i] === sep[0] && sep.length > 1) {
        
    }
}
split("soufian--sou--so--", "--")